import styled from 'styled-components';

import { colors } from '@namespace/web-shared/styles/theme';

export const TemplateTitle = styled.h3`
  font-size: 32px;
  line-height: 42px;
  color: ${colors.blackH303234};
  text-align: center;
`;

export const TemplateWrapper = styled.div`
  position: relative;
`;
